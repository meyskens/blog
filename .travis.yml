language: go
go:
  - 1.x
addons:
  ssh_known_hosts: eyskens.me
  apt:
    packages:
        - sshpass
        - wget 
sudo: required

install:
  - export SSHPASS=$DEPLOY_PASS
  - wget https://github.com/spf13/hugo/releases/download/v0.37.1/hugo_0.37.1-64bit.deb
  - sudo dpkg -i hugo_0.37.1-64bit.deb

script:
  - hugo

after_success:
- sshpass -e rsync -avz --delete public/ blog@eyskens.me:/var/www/blog/